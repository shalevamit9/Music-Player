<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/dark_blue"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:context=".MainPlayerFragment">

    <ImageView
        android:id="@+id/my_music_player_iv"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:scaleType="centerCrop"
        android:src="@drawable/main_player_fragment_title"/>
    
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/songs_rv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/my_music_player_iv"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/add_song_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="15dp"
        android:src="@drawable/ic_baseline_add_24"
        android:backgroundTint="@color/orange"
        android:layout_alignBottom="@id/songs_rv"
        android:layout_alignEnd="@id/songs_rv"/>

</RelativeLayout>